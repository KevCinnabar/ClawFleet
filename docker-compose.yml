services:
  managers:
    build:
      context: manager
    container_name: claw-manager
    restart: unless-stopped
    ports:
      - "3001:3000"
    env_file:
      - ./.env
      - manager/config/manager.env
    volumes:
      - ./data/managers:/data
    networks:
      - fleet-net

networks:
  fleet-net:
    driver: bridge